# Copy this file to backend.tf after creating the S3 bucket and DynamoDB table.
# Keeping it as an example avoids making `terraform init` fail for reviewers who
# have not created remote state infrastructure yet.
#
# terraform {
#   backend "s3" {
#     bucket         = "alchemyst-tf-state-ACCOUNT_ID"
#     key            = "devops-assignment/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "alchemyst-tf-lock"
#   }
# }
